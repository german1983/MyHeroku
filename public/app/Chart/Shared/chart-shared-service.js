(function (angular, $) {
    "use strict";

    angular.module("Chart").service("$chartSharedService", ['$http', chartSharedService]);

    function chartSharedService($http) {
        var $scope;
        var serviceScope = this;

    };
})(angular, jQuery);
