(function (angular, $) {
    "use strict";

    angular.module("Chart").service("$chartLaunchService", ['$http', chartLaunchService]);

    function chartLaunchService($http) {
        var $scope;
        var serviceScope = this;

    };
})(angular, jQuery);
